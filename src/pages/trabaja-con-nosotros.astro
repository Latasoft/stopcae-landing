---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import WhatsAppButton from '../components/WhatsAppButton.astro';
---

<Layout>
    <Header />
    
    <main class="trabaja-page">
        <section class="trabaja-header">
            <div class="banner-container">
                <div class="banner-illustration">
                    <div class="illustration-placeholder">
                        <svg width="200" height="200" viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <circle cx="100" cy="100" r="80" fill="#E0DCDC" stroke="#CA0101" stroke-width="2"/>
                            <path d="M70 90 L90 110 L130 70" stroke="#CA0101" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </div>
                </div>
                <div class="banner-content">
                    <h1 class="banner-title">Únete a Nuestro Equipo</h1>
                    <p class="banner-subtitle">Forma parte de un estudio jurídico que transforma vidas</p>
                </div>
            </div>
        </section>
        
        <section class="trabaja-content-section">
            <div class="container">
                <div class="form-header">
                    <h2>Postula a Nuestro Equipo</h2>
                    <p>Completa el formulario y envíanos tu información para formar parte de nuestro estudio jurídico</p>
                </div>
                
                <form class="postulacion-form" id="postulacionForm">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="nombre">Nombre Completo *</label>
                            <input type="text" id="nombre" name="nombre" required placeholder="Ej. María Pérez">
                        </div>
                        <div class="form-group">
                            <label for="email">Correo Electrónico *</label>
                            <input type="email" id="email" name="email" required placeholder="Ej. correo@ejemplo.com">
                        </div>
                    </div>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label for="telefono">Teléfono</label>
                            <input type="tel" id="telefono" name="telefono" placeholder="+56 9 1234 5678">
                        </div>
                        <div class="form-group">
                            <label for="ciudad">Ciudad *</label>
                            <input type="text" id="ciudad" name="ciudad" required placeholder="Ej. Santiago">
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="especialidad">Especialidad Legal *</label>
                        <select id="especialidad" name="especialidad" required>
                            <option value="">Selecciona tu especialidad</option>
                            <option value="derecho-civil">Derecho Civil</option>
                            <option value="derecho-comercial">Derecho Comercial</option>
                            <option value="derecho-consumo">Derecho de Consumo</option>
                            <option value="procedimientos-judiciales">Procedimientos Judiciales</option>
                            <option value="otra">Otra especialidad</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="experiencia">Años de Experiencia *</label>
                        <select id="experiencia" name="experiencia" required>
                            <option value="">Selecciona tu experiencia</option>
                            <option value="0-1">0-1 años</option>
                            <option value="2-3">2-3 años</option>
                            <option value="4-5">4-5 años</option>
                            <option value="6-10">6-10 años</option>
                            <option value="10+">Más de 10 años</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="motivacion">¿Por qué quieres unirte a nuestro equipo? *</label>
                        <textarea id="motivacion" name="motivacion" rows="4" required placeholder="Describe brevemente tu motivación (máx. 300 caracteres)"></textarea>
                    </div>
                    
                    <div class="form-group">
                        <label for="aportes">¿Cómo crees que puedes contribuir a nuestra misión? *</label>
                        <textarea id="aportes" name="aportes" rows="4" required placeholder="Ej. experiencia en litigios de consumo, negociación con entidades financieras..."></textarea>
                    </div>
                    
                    <div class="form-group">
                        <label for="cv">Adjuntar CV (PDF) *</label>
                        <input type="file" id="cv" name="cv" accept=".pdf" required>
                        <small>Máximo 5MB. Solo archivos PDF.</small>
                    </div>
                    
                    <div class="form-group">
                        <label for="carta">Carta de Presentación (opcional)</label>
                        <textarea id="carta" name="carta" rows="3" placeholder="Carta breve (opcional): ¿por qué encajas en el equipo?"></textarea>
                    </div>
                    
                    <div class="form-group checkbox-group">
                        <label class="checkbox-label">
                            <input type="checkbox" name="disponibilidad" required>
                            <span class="checkmark" aria-hidden="true"></span>
                            Estoy disponible para comenzar en el plazo indicado
                        </label>
                    </div>
                    
                    <div class="form-group checkbox-group">
                        <label class="checkbox-label">
                            <input type="checkbox" name="acepto" required>
                            <span class="checkmark" aria-hidden="true"></span>
                            Acepto el procesamiento de mis datos personales según la política de privacidad
                        </label>
                    </div>
                    
                    <button type="submit" class="submit-btn">
                        <span class="btn-text">Enviar Postulación</span>
                        <span class="btn-icon">📤</span>
                    </button>
                </form>
            </div>
        </section>
    </main>
    
    <Footer />
    <WhatsAppButton />
</Layout>

<style>
    .trabaja-page {
        min-height: 100vh;
        background: linear-gradient(135deg, rgb(248, 249, 250) 0%, rgb(233, 236, 239) 100%);
        scroll-margin-top: 120px;
        position: relative;
        overflow-x: hidden;
    }

    .trabaja-page::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: 
            radial-gradient(circle at 20% 20%, rgba(202, 1, 1, 0.03) 0%, transparent 50%),
            radial-gradient(circle at 80% 80%, rgba(227, 6, 19, 0.02) 0%, transparent 50%),
            radial-gradient(circle at 40% 60%, rgba(202, 1, 1, 0.01) 0%, transparent 50%);
        pointer-events: none;
        z-index: 0;
        animation: backgroundFloat 20s ease-in-out infinite;
    }

    @keyframes backgroundFloat {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        33% { transform: translateY(-10px) rotate(0.5deg); }
        66% { transform: translateY(5px) rotate(-0.5deg); }
    }

    .trabaja-header {
        padding: clamp(8rem, 20vh, 14rem) 0 2rem 0; /* Aumentado desde 6rem a 8rem */
        background: linear-gradient(135deg, #393939 0%, #2d2d2d 100%);
        position: relative;
        overflow: hidden;
        min-height: 50vh;
        display: flex;
        align-items: center;
    }

    .trabaja-header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: 
            radial-gradient(circle at 20% 30%, rgba(202, 1, 1, 0.1) 0%, transparent 50%),
            radial-gradient(circle at 80% 70%, rgba(227, 6, 19, 0.08) 0%, transparent 50%);
        pointer-events: none;
    }

    .banner-container {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        gap: 2rem;
        padding: 0 2rem;
        position: relative;
        z-index: 2;
        opacity: 1;
        transform: translateY(0);
    }

    @media (min-width: 769px) {
        .banner-container {
            opacity: 0;
            transform: translateY(50px);
            animation: bannerSlideIn 1.2s ease-out 0.3s forwards;
        }
    }

    @keyframes bannerSlideIn {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .banner-illustration {
        flex: 0 0 300px;
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 1;
        transform: scale(1) rotate(0deg);
    }

    @media (min-width: 769px) {
        .banner-illustration {
            opacity: 0;
            transform: scale(0.8) rotate(-10deg);
            animation: illustrationFloat 1.5s ease-out 0.6s forwards;
        }
    }

    @keyframes illustrationFloat {
        to {
            opacity: 1;
            transform: scale(1) rotate(0deg);
        }
    }

    .illustration-placeholder {
        width: 200px;
        height: 200px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        backdrop-filter: blur(10px);
        border: 2px solid rgba(202, 1, 1, 0.3);
        transition: all 0.4s ease;
        position: relative;
        overflow: hidden;
    }

    .illustration-placeholder::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
        transform: rotate(45deg);
        transition: all 0.6s ease;
        opacity: 0;
    }

    .illustration-placeholder:hover::before {
        opacity: 1;
        animation: shimmer 1.5s ease-in-out;
    }

    .illustration-placeholder:hover {
        transform: scale(1.05);
        border-color: rgba(202, 1, 1, 0.6);
        box-shadow: 0 0 30px rgba(202, 1, 1, 0.3);
    }

    @keyframes shimmer {
        0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
        100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
    }

    .banner-content {
        flex: 1;
        color: white;
    }

    .banner-title {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 800;
        line-height: 1.2;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.8);
        position: relative;
        opacity: 1;
        transform: translateX(0);
    }

    @media (min-width: 769px) {
        .banner-title {
            opacity: 0;
            transform: translateX(-30px);
            animation: titleSlideIn 1s ease-out 0.9s forwards;
        }
    }

    @keyframes titleSlideIn {
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    .banner-title::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 0;
        width: 100px;
        height: 5px;
        background: linear-gradient(90deg, #CA0101, #E30613);
        border-radius: 3px;
    }

    @media (min-width: 769px) {
        .banner-title::after {
            width: 0;
            animation: underlineExpand 1s ease-out 1.6s forwards;
        }
    }

    @keyframes underlineExpand {
        to {
            width: 100px;
        }
    }

    .banner-subtitle {
        font-size: clamp(1.2rem, 2.5vw, 1.8rem);
        color: #E0DCDC;
        opacity: 0.9;
        line-height: 1.4;
        opacity: 1;
        transform: translateY(0);
    }

    @media (min-width: 769px) {
        .banner-subtitle {
            opacity: 0;
            transform: translateY(20px);
            animation: subtitleSlideIn 0.8s ease-out 1.2s forwards;
        }
    }

    @keyframes subtitleSlideIn {
        to {
            opacity: 0.9;
            transform: translateY(0);
        }
    }

    .trabaja-header::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        height: 10px;
        background: linear-gradient(90deg, #CA0101, #E30613, #E0DCDC);
    }

    .trabaja-content-section {
        padding: 6rem 0;
        background: linear-gradient(135deg, rgb(248, 249, 250) 0%, rgb(233, 236, 239) 100%);
        color: #393939;
        position: relative;
        overflow: hidden;
        border: 1px solid rgba(202, 1, 1, 0.08);
    }

    .trabaja-content-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: 
            radial-gradient(circle at 20% 30%, rgba(202, 1, 1, 0.08) 0%, transparent 50%),
            radial-gradient(circle at 80% 70%, rgba(227, 6, 19, 0.06) 0%, transparent 50%);
        pointer-events: none;
        z-index: 0;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
        position: relative;
        z-index: 2;
    }

    .form-header {
        text-align: center;
        margin-bottom: 3rem;
        max-width: 800px;
        margin-left: auto;
        margin-right: auto;
    }

    .form-header h2 {
        color: #CA0101;
        font-size: clamp(2.5rem, 4vw, 3.5rem);
        font-weight: 700;
        margin-bottom: 1rem;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        position: relative;
    }

    .form-header h2::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 4px;
        background: linear-gradient(90deg, #CA0101, #E30613);
        border-radius: 2px;
    }

    .form-header p {
        color: #393939;
        font-size: 1.2rem;
        line-height: 1.6;
        opacity: 0.8;
    }

    .postulacion-form {
        background: rgba(255, 255, 255, 0.98);
        padding: 3rem;
        border-radius: 24px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.08);
        border: 1px solid rgba(202, 1, 1, 0.08);
        max-width: 800px;
        margin: 0 auto;
        position: relative;
        overflow: visible;
        backdrop-filter: blur(10px);
        z-index: 2;
        opacity: 1;
        transform: translateY(0) scale(1);
        transition: all 0.3s ease;
    }

    @media (min-width: 769px) {
        .postulacion-form {
            opacity: 0;
            transform: translateY(50px) scale(0.95);
            animation: formSlideIn 1.2s ease-out 0.5s forwards;
        }
    }

    .postulacion-form:hover {
        transform: translateY(-5px) scale(1.01);
        box-shadow: 0 30px 80px rgba(0, 0, 0, 0.12);
    }

    @keyframes formSlideIn {
        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    .postulacion-form::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #CA0101, #E30613);
        z-index: 0;
    }

    .form-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .form-group {
        margin-bottom: 1.5rem;
        position: relative;
        z-index: 3;
    }

    .form-group label {
        display: block;
        color: #393939;
        font-weight: 600;
        margin-bottom: 0.5rem;
        font-size: 1rem;
    }

    .form-group input,
    .form-group select,
    .form-group textarea {
        width: 100%;
        padding: 1rem;
        border: 2px solid rgba(202, 1, 1, 0.1);
        border-radius: 12px;
        font-size: 1rem;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        background: rgba(255, 255, 255, 0.9);
        color: #393939;
        box-sizing: border-box;
        display: block;
        position: relative;
        z-index: 1;
        transform: translateY(0);
    }

    .form-group input:focus,
    .form-group select:focus,
    .form-group textarea:focus {
        outline: none;
        border-color: #CA0101;
        box-shadow: 0 0 0 3px rgba(202, 1, 1, 0.1), 0 8px 25px rgba(202, 1, 1, 0.15);
        transform: translateY(-2px);
        background: rgba(255, 255, 255, 1);
    }

    .form-group input:hover,
    .form-group select:hover,
    .form-group textarea:hover {
        border-color: rgba(202, 1, 1, 0.3);
        transform: translateY(-1px);
        box-shadow: 0 4px 15px rgba(202, 1, 1, 0.1);
    }



    .form-group input[type="file"] {
        padding: 0.75rem;
        border: 2px dashed rgba(202, 1, 1, 0.2);
        background: rgba(202, 1, 1, 0.02);
        cursor: pointer;
    }

    .form-group input[type="file"]:hover {
        border-color: #CA0101;
        background: rgba(202, 1, 1, 0.05);
    }

    .form-group small {
        display: block;
        color: #666;
        font-size: 0.85rem;
        margin-top: 0.5rem;
        font-style: italic;
    }

    .checkbox-group {
        margin-bottom: 2rem;
    }

    /* Checkbox — accessible custom checkbox */
    .checkbox-label {
        display: flex;
        align-items: center;
        gap: 1rem;
        cursor: pointer;
        color: #393939;
        font-size: 1rem;
        line-height: 1.5;
        position: relative;
    }

    /* hide native checkbox but keep it accessible */
    .checkbox-label input[type="checkbox"] {
        position: absolute;
        opacity: 0;
        width: 1px;
        height: 1px;
        margin: 0;
        overflow: hidden;
        clip: rect(0 0 0 0);
        clip-path: inset(50%);
        white-space: nowrap;
    }

    .checkmark {
        position: relative;
        display: inline-block;
        width: 20px;
        height: 20px;
        background: rgba(255, 255, 255, 0.9);
        border: 2px solid rgba(202, 1, 1, 0.2);
        border-radius: 4px;
        transition: all 0.18s ease;
        flex-shrink: 0;
    }

    .checkbox-label:hover .checkmark {
        border-color: #CA0101;
        background: rgba(202, 1, 1, 0.04);
    }

    /* check mark (hidden by default) */
    .checkmark::after {
        content: '';
        position: absolute;
        left: 5px;
        top: 1px;
        width: 6px;
        height: 11px;
        border: solid white;
        border-width: 0 2px 2px 0;
        transform: rotate(45deg) scale(0.6);
        opacity: 0;
        transition: all 0.16s ease;
    }

    /* checked state */
    .checkbox-label input[type="checkbox"]:checked + .checkmark {
        background: linear-gradient(135deg, #CA0101 0%, #E30613 100%);
        border-color: #CA0101;
    }

    .checkbox-label input[type="checkbox"]:checked + .checkmark::after {
        opacity: 1;
        transform: rotate(45deg) scale(1);
    }
    
    /* keyboard focus */
    .checkbox-label input[type="checkbox"]:focus + .checkmark {
        box-shadow: 0 0 0 4px rgba(202,1,1,0.08);
    }

    .submit-btn {
        width: 100%;
        background: linear-gradient(135deg, #CA0101, #E30613);
        color: white;
        border: none;
        padding: 1.5rem 2rem;
        border-radius: 16px;
        font-size: 1.2rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        cursor: pointer;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 1rem;
        box-shadow: 0 8px 25px rgba(202, 1, 1, 0.3);
        position: relative;
        overflow: hidden;
        opacity: 1;
        transform: translateY(0) scale(1);
    }

    @media (min-width: 769px) {
        .submit-btn {
            opacity: 0;
            transform: translateY(30px) scale(0.9);
            animation: buttonSlideIn 1s ease-out 1.8s forwards;
        }
    }

    .submit-btn:focus {
        outline: 3px solid rgba(202, 1, 1, 0.3);
        outline-offset: 2px;
    }

    .submit-btn:disabled {
        opacity: 0.7;
        cursor: not-allowed;
        transform: scale(0.98);
    }

    @keyframes buttonSlideIn {
        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    .submit-btn::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        transition: left 0.5s ease;
    }

    .submit-btn:hover::before {
        left: 100%;
    }

    .submit-btn:hover {
        transform: translateY(-5px) scale(1.02);
        box-shadow: 0 15px 40px rgba(202, 1, 1, 0.5);
        background: linear-gradient(135deg, #E30613, #CA0101);
    }

    .submit-btn:active {
        transform: translateY(-1px);
    }

    .btn-text {
        position: relative;
        z-index: 2;
    }

    .btn-icon {
        font-size: 1.5rem;
        position: relative;
        z-index: 2;
        animation: bounce 2s infinite;
    }

    @keyframes bounce {
        0%, 20%, 50%, 80%, 100% {
            transform: translateY(0);
        }
        40% {
            transform: translateY(-5px);
        }
        60% {
            transform: translateY(-3px);
        }
    }

    @media (max-width: 768px) {
        .banner-container {
            flex-direction: column;
            text-align: center;
            gap: 2rem;
        }

        .banner-illustration {
            flex: none;
        }

        .banner-title::after {
            left: 50%;
            transform: translateX(-50%);
        }

        .trabaja-header {
            padding-top: clamp(10rem, 25vh, 16rem); /* Padding específico para móvil */
            padding-bottom: 1rem;
        }

        .trabaja-content-section {
            padding: 4rem 0;
        }

        .form-header h2 {
            font-size: clamp(2rem, 5vw, 2.5rem);
            margin-bottom: 1rem;
        }

        .form-header p {
            font-size: 1rem;
            line-height: 1.5;
        }

        .postulacion-form {
            padding: 2rem 1.5rem;
            margin: 0 1rem;
        }

        .form-row {
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            padding: 0.875rem;
            font-size: 0.95rem;
        }

        .submit-btn {
            padding: 1.25rem 1.5rem;
            font-size: 1.1rem;
        }

        .btn-icon {
            font-size: 1.3rem;
        }
    }
</style>

<script>
    // Importar EmailJS y configuración
    import emailjs from '@emailjs/browser';
    import { EMAILJS_CONFIG, validateEmailJSConfig } from '../config/emailjs.js';

    // Manejo del formulario de postulación
    document.addEventListener('DOMContentLoaded', function() {
        const form = document.getElementById('postulacionForm');
        
        if (form) {
            form.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                // Obtener el botón de envío
                const submitBtn = form.querySelector('.submit-btn');
                const originalText = submitBtn.innerHTML;
                
                // Cambiar estado del botón a "enviando"
                submitBtn.innerHTML = '<span class="btn-text">Enviando...</span><span class="btn-icon">⏳</span>';
                submitBtn.disabled = true;
                
                try {
                    // Recopilar datos del formulario
                    const formData = {
                        nombre: form.querySelector('[name="nombre"]').value,
                        email: form.querySelector('[name="email"]').value,
                        telefono: form.querySelector('[name="telefono"]').value || 'No proporcionado',
                        ciudad: form.querySelector('[name="ciudad"]').value,
                        especialidad: form.querySelector('[name="especialidad"]').value,
                        experiencia: form.querySelector('[name="experiencia"]').value,
                        motivacion: form.querySelector('[name="motivacion"]').value,
                        aportes: form.querySelector('[name="aportes"]').value,
                        carta: form.querySelector('[name="carta"]').value || 'Sin carta de presentación',
                        disponibilidad: form.querySelector('[name="disponibilidad"]').checked ? 'Sí' : 'No',
                        time: new Date().toLocaleString('es-CL', {
                            year: 'numeric',
                            month: 'long',
                            day: 'numeric',
                            hour: '2-digit',
                            minute: '2-digit'
                        })
                    };

                    // Validar configuración antes de enviar
                    if (!validateEmailJSConfig()) {
                        throw new Error('Configuración de EmailJS incompleta');
                    }

                    // Enviar email usando EmailJS
                    const response = await emailjs.send(
                        EMAILJS_CONFIG.SERVICE_ID,
                        EMAILJS_CONFIG.TEMPLATE_ID_TRABAJA,
                        formData,
                        EMAILJS_CONFIG.PUBLIC_KEY
                    );

                    if (response.status === 200) {
                        // Éxito
                        alert('¡Gracias por tu postulación! Hemos recibido tu información y nos pondremos en contacto contigo pronto.');
                        form.reset();
                        
                        // Limpiar validaciones visuales
                        form.querySelectorAll('input, select, textarea').forEach(field => {
                            field.style.borderColor = 'rgba(202, 1, 1, 0.1)';
                        });
                    } else {
                        throw new Error('Error en el envío');
                    }
                    
                } catch (error) {
                    console.error('Error al enviar el formulario:', error);
                    alert('Hubo un error al enviar tu postulación. Por favor, intenta nuevamente.');
                } finally {
                    // Restaurar botón
                    submitBtn.innerHTML = originalText;
                    submitBtn.disabled = false;
                }
            });
        }
        
        // Validación en tiempo real
        const requiredFields = form.querySelectorAll('[required]');
        requiredFields.forEach(field => {
            field.addEventListener('blur', function() {
                if (!this.value.trim()) {
                    this.style.borderColor = '#E30613';
                } else {
                    this.style.borderColor = 'rgba(202, 1, 1, 0.1)';
                }
            });
        });
    });
</script>
