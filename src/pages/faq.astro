---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import WhatsAppButton from '../components/WhatsAppButton.astro';
---

<Layout>
    <Header />
    
    <main class="faq-page">
        <section class="faq-header">
            <div class="banner-container">
                <div class="banner-illustration">
                    <div class="illustration-placeholder">
                        <svg width="200" height="200" viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <circle cx="100" cy="100" r="80" fill="#E0DCDC" stroke="#CA0101" stroke-width="2"/>
                            <path d="M70 90 L90 110 L130 70" stroke="#CA0101" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </div>
                </div>
                <div class="banner-content">
                    <h1 class="banner-title">Preguntas Frecuentes sobre CAE</h1>
                    <p class="banner-subtitle">Resolvemos todas tus dudas sobre deudas estudiantiles</p>
                </div>
            </div>
        </section>
        
        <section class="faq-content-section">
            <div class="container">

                
                <div class="faq-grid">
                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Cómo saber si estoy demandado(a) por no pagar mi CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-search"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>Si ya pasaron más de 3 meses desde que dejaste de pagar, es posible que el banco te haya demandado. Para revisarlo:<br>
                            1. Ingresa a Oficina Judicial Virtual.<br>
                            2. Entra con tu RUT y ClaveÚnica.<br>
                            3. Selecciona "Mis Causas" → "Civil".<br>
                            Si no aparece tu demanda, igual puede existir. ¡Completa nuestro formulario y la buscamos por ti!</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Prescribe la deuda del CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-clock"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>La deuda del CAE no prescribe. Lo que sí prescribe es el pagaré (el documento que usan para cobrarte). Nosotros podemos ayudarte a que ese pagaré prescriba.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Qué pasa si el banco me demanda por el CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-gavel"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>Un Receptor Judicial irá a tu domicilio para notificarte. No esperes a que llegue: ¡Contáctanos antes!</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Se puede eliminar el CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-eraser"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>No se puede borrar la deuda, pero sí existen estrategias legales que pueden liberarte del pago. Nuestro equipo se encarga de eso.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Qué hacer si me llega la orden de embargo por CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-exclamation-triangle"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>No vayas al banco ni a la citación. Sácale una foto a la carta y mándanosla de inmediato. Nosotros podemos defenderte y proteger tu patrimonio.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Cuánto es el interés por CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-percentage"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>Hoy el interés es de 2% anual (antes era 6%). Pero está en UF, lo que significa que la deuda sube todos los días.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Cómo hacer para no pagar el CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-shield-alt"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>Existen defensas legales que pueden liberarte del CAE en cualquier etapa. Cuéntanos tu caso y te diremos la mejor opción.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Cómo postular a la condonación del CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-file-contract"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>No existe condonación oficial del CAE. Lo que sí existe son defensas legales para dejar de pagarlo.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Qué pasa con el CAE si estoy cesante?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-user-clock"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>El banco puede darte suspensión de pagos, pero eso solo aumenta tu deuda porque sigue en UF. Si estás cesante, contáctanos para buscar una solución real.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Qué pasa si no puedo pagar el CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-hand-holding-usd"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>Si no pagas, después de 3 meses el banco puede demandarte. Lo mejor es asesorarte antes. Escríbenos y te explicamos cómo dejar de pagar de forma segura.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Puedo defenderme del CAE si vivo fuera de Chile?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-globe-americas"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>Sí. Hemos ayudado a personas en el extranjero a resolver su deuda.</p>
                        </div>
                    </div>

                    <div class="faq-item">
                        <div class="faq-question">
                            <h3>¿Qué debo hacer si me notifican mi demanda del CAE?</h3>
                            <div class="faq-icon">
                                <i class="fas fa-exclamation-circle"></i>
                            </div>
                        </div>
                        <div class="faq-answer">
                            <p>No vayas a la citación (solo te informarán de la deuda). Sácale fotos a todos los documentos y avísanos de inmediato. Nuestro equipo puede defenderte.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>
    
    <Footer />
    <WhatsAppButton />
</Layout>

<style>
    .faq-page {
        min-height: 100vh;
        background: linear-gradient(135deg, rgb(248, 249, 250) 0%, rgb(233, 236, 239) 100%);
        scroll-margin-top: 120px;
        position: relative;
        overflow-x: hidden;
    }

    .faq-page::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: 
            radial-gradient(circle at 20% 20%, rgba(202, 1, 1, 0.03) 0%, transparent 50%),
            radial-gradient(circle at 80% 80%, rgba(227, 6, 19, 0.02) 0%, transparent 50%),
            radial-gradient(circle at 40% 60%, rgba(202, 1, 1, 0.01) 0%, transparent 50%);
        pointer-events: none;
        z-index: 0;
        animation: backgroundFloat 20s ease-in-out infinite;
    }

    @keyframes backgroundFloat {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        33% { transform: translateY(-10px) rotate(0.5deg); }
        66% { transform: translateY(5px) rotate(-0.5deg); }
    }

    .faq-header {
        padding: clamp(8rem, 20vh, 14rem) 0 2rem 0; /* Aumentado desde 6rem a 8rem */
        background: linear-gradient(135deg, #393939 0%, #2d2d2d 100%);
        position: relative;
        overflow: hidden;
        min-height: 50vh;
        display: flex;
        align-items: center;
    }

    .faq-header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: 
            radial-gradient(circle at 20% 30%, rgba(202, 1, 1, 0.1) 0%, transparent 50%),
            radial-gradient(circle at 80% 70%, rgba(227, 6, 19, 0.08) 0%, transparent 50%);
        pointer-events: none;
    }

    .banner-container {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        gap: 2rem;
        padding: 0 2rem;
        position: relative;
        z-index: 2;
        opacity: 1;
        transform: translateY(0);
    }

    @media (min-width: 769px) {
        .banner-container {
            opacity: 0;
            transform: translateY(50px);
            animation: bannerSlideIn 1.2s ease-out 0.3s forwards;
        }
    }

    @keyframes bannerSlideIn {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .banner-illustration {
        flex: 0 0 300px;
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 1;
        transform: scale(1) rotate(0deg);
    }

    @media (min-width: 769px) {
        .banner-illustration {
            opacity: 0;
            transform: scale(0.8) rotate(-10deg);
            animation: illustrationFloat 1.5s ease-out 0.6s forwards;
        }
    }

    @keyframes illustrationFloat {
        to {
            opacity: 1;
            transform: scale(1) rotate(0deg);
        }
    }

    .illustration-placeholder {
        width: 200px;
        height: 200px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        backdrop-filter: blur(10px);
        border: 2px solid rgba(202, 1, 1, 0.3);
        transition: all 0.4s ease;
        position: relative;
        overflow: hidden;
    }

    .illustration-placeholder::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
        transform: rotate(45deg);
        transition: all 0.6s ease;
        opacity: 0;
    }

    .illustration-placeholder:hover::before {
        opacity: 1;
        animation: shimmer 1.5s ease-in-out;
    }

    .illustration-placeholder:hover {
        transform: scale(1.05);
        border-color: rgba(202, 1, 1, 0.6);
        box-shadow: 0 0 30px rgba(202, 1, 1, 0.3);
    }

    @keyframes shimmer {
        0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
        100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
    }

    .banner-content {
        flex: 1;
        color: white;
    }

    .banner-title {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 800;
        line-height: 1.2;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.8);
        position: relative;
        opacity: 1;
        transform: translateX(0);
    }

    @media (min-width: 769px) {
        .banner-title {
            opacity: 0;
            transform: translateX(-30px);
            animation: titleSlideIn 1s ease-out 0.9s forwards;
        }
    }

    @keyframes titleSlideIn {
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    .banner-title::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 0;
        width: 100px;
        height: 5px;
        background: linear-gradient(90deg, #CA0101, #E30613);
        border-radius: 3px;
    }

    @media (min-width: 769px) {
        .banner-title::after {
            width: 0;
            animation: underlineExpand 1s ease-out 1.6s forwards;
        }
    }

    @keyframes underlineExpand {
        to {
            width: 100px;
        }
    }

    .banner-subtitle {
        font-size: clamp(1.2rem, 2.5vw, 1.8rem);
        color: #E0DCDC;
        opacity: 0.9;
        line-height: 1.4;
        opacity: 1;
        transform: translateY(0);
    }

    @media (min-width: 769px) {
        .banner-subtitle {
            opacity: 0;
            transform: translateY(20px);
            animation: subtitleSlideIn 0.8s ease-out 1.2s forwards;
        }
    }

    @keyframes subtitleSlideIn {
        to {
            opacity: 0.9;
            transform: translateY(0);
        }
    }

    .faq-header::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        height: 10px;
        background: linear-gradient(90deg, #CA0101, #E30613, #E0DCDC);
    }

    .faq-content-section {
        padding: 6rem 0;
        background: linear-gradient(135deg, rgb(248, 249, 250) 0%, rgb(233, 236, 239) 100%);
        color: #393939;
        position: relative;
        overflow: hidden;
        border: 1px solid rgba(202, 1, 1, 0.08);
    }

    .faq-content-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: 
            radial-gradient(circle at 20% 30%, rgba(202, 1, 1, 0.08) 0%, transparent 50%),
            radial-gradient(circle at 80% 70%, rgba(227, 6, 19, 0.06) 0%, transparent 50%);
        pointer-events: none;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
        position: relative;
        z-index: 2;
    }

    @media (min-width: 769px) {
        .faq-content-section .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }
    }

    .section-title {
        text-align: center;
        font-size: clamp(2.5rem, 4vw, 3.5rem);
        color: #F5F5F5;
        margin-bottom: 4rem;
        font-weight: 700;
        position: relative;
    }

    .section-title::after {
        content: '';
        position: absolute;
        bottom: -15px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 4px;
        background: linear-gradient(90deg, #CA0101, #E30613);
        border-radius: 2px;
    }



    .faq-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 2rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    @media (min-width: 769px) {
        .faq-grid {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }
    }

    .faq-item {
        background: rgba(255, 255, 255, 0.98);
        padding: 3.5rem 2.5rem;
        border-radius: 24px;
        text-align: center;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.08);
        border: 1px solid rgba(202, 1, 1, 0.08);
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        position: relative;
        overflow: hidden;
        backdrop-filter: blur(10px);
        opacity: 1;
        transform: translateY(0) scale(1);
    }

    @media (min-width: 769px) {
        .faq-item {
            opacity: 0;
            transform: translateY(50px) scale(0.9);
            animation: faqSlideIn 0.8s ease-out forwards;
        }

        .faq-item:nth-child(1) { animation-delay: 0.1s; }
        .faq-item:nth-child(2) { animation-delay: 0.2s; }
        .faq-item:nth-child(3) { animation-delay: 0.3s; }
        .faq-item:nth-child(4) { animation-delay: 0.4s; }
        .faq-item:nth-child(5) { animation-delay: 0.5s; }
        .faq-item:nth-child(6) { animation-delay: 0.6s; }
        .faq-item:nth-child(7) { animation-delay: 0.7s; }
        .faq-item:nth-child(8) { animation-delay: 0.8s; }
        .faq-item:nth-child(9) { animation-delay: 0.9s; }
        .faq-item:nth-child(10) { animation-delay: 1.0s; }
        .faq-item:nth-child(11) { animation-delay: 1.1s; }
        .faq-item:nth-child(12) { animation-delay: 1.2s; }
    }

    @keyframes faqSlideIn {
        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    @media (min-width: 769px) {
        .faq-item {
            width: 100%;
            text-align: left;
            padding: 2rem 2.5rem;
        }

        .faq-question {
            flex-direction: row;
            align-items: center;
            margin-bottom: 0;
        }

        .faq-answer {
            padding-left: calc(40px + 2rem);
        }
    }

    .faq-item::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #CA0101, #E30613);
        transform: scaleX(0);
        transition: transform 0.4s ease;
    }

    .faq-item:hover::before {
        transform: scaleX(1);
    }

    .faq-item:hover {
        transform: translateY(-12px) scale(1.02);
        box-shadow: 0 30px 80px rgba(0, 0, 0, 0.12);
        border-color: rgba(202, 1, 1, 0.15);
    }

    .faq-question {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.5rem;
        margin-bottom: 2rem;
        position: relative;
    }

    .faq-question::after {
        content: '+';
        position: absolute;
        top: 50%;
        right: 0;
        transform: translateY(-50%);
        font-size: 2rem;
        color: #CA0101;
        font-weight: bold;
        transition: all 0.3s ease;
    }

    .faq-item.active .faq-question::after {
        content: '−';
        transform: translateY(-50%);
    }

    .faq-question h3 {
        color: #CA0101;
        font-size: 1.8rem;
        font-weight: 700;
        margin: 0;
        line-height: 1.3;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        position: relative;
        padding-right: 2.5rem;
    }

    .faq-question h3::after {
        content: '';
        position: absolute;
        bottom: -8px;
        left: 50%;
        transform: translateX(-50%);
        width: 40px;
        height: 3px;
        background: linear-gradient(90deg, #CA0101, #E30613);
        border-radius: 2px;
        opacity: 0;
        transition: all 0.3s ease;
    }

    .faq-item:hover .faq-question h3::after {
        opacity: 1;
        width: 60px;
    }

    .faq-icon {
        font-size: 2.5rem;
        color: #CA0101;
        opacity: 0.8;
        transition: all 0.3s ease;
        filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
    }
    
    .faq-icon i {
        font-size: 2.5rem;
        color: #CA0101;
        transition: all 0.3s ease;
    }
    
    .faq-icon::after {
        content: '\f107';
        font-family: 'Font Awesome 5 Free';
        font-weight: 900;
        font-size: 1.5rem;
        position: absolute;
        bottom: -0.5rem;
        color: #CA0101;
        opacity: 0.8;
        transition: transform 0.3s ease;
    }
    
    .faq-item.active .faq-icon::after {
        transform: rotate(180deg);
    }

    .faq-item:hover .faq-icon {
        opacity: 1;
        transform: scale(1.1);
        filter: drop-shadow(0 6px 12px rgba(0, 0, 0, 0.15));
    }

    .faq-answer {
        max-height: 0;
        overflow: hidden;
        transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        opacity: 0;
    }

    .faq-answer p {
        color: #393939;
        line-height: 1.8;
        margin: 0;
        font-size: 1.1rem;
        text-align: left;
        padding: 1rem 0;
    }

    .faq-item.active .faq-answer {
        max-height: 500px;
        opacity: 1;
        margin-top: 1rem;
    }

    .faq-icon i {
        transition: transform 0.3s ease;
    }

    .faq-item.active .faq-icon i {
        transform: rotate(180deg);
    }

    .faq-item {
        cursor: pointer;
    }

    .faq-item:hover {
        transform: translateY(-5px);
    }

    @media (max-width: 768px) {
        .banner-container {
            flex-direction: column;
            text-align: center;
            gap: 2rem;
        }

        .banner-illustration {
            flex: none;
        }

        .banner-title::after {
            left: 50%;
            transform: translateX(-50%);
        }

        .faq-header {
            padding-top: clamp(10rem, 25vh, 16rem); /* Padding específico para móvil */
            padding-bottom: 1rem;
        }

        .faq-content-section {
            padding: 4rem 0;
        }

        .section-title {
            font-size: clamp(2rem, 5vw, 2.5rem);
            margin-bottom: 3rem;
        }

        .faq-intro-grid {
            grid-template-columns: 1fr;
            gap: 2rem;
            margin-bottom: 3rem;
        }
        
        .faq-intro-card {
            padding: 2.5rem 1.5rem;
        }
        
        .faq-intro-card h3 {
            font-size: 1.4rem;
            margin-bottom: 1.5rem;
        }
        
        .faq-intro-card p {
            font-size: 1rem;
            line-height: 1.6;
        }
        
        .faq-intro-card .card-icon {
            font-size: 3rem;
            margin-bottom: 1.5rem;
        }

        .faq-grid {
            grid-template-columns: 1fr;
            gap: 2rem;
            padding: 0 1rem;
        }

        .faq-item {
            padding: 2.5rem 1.5rem;
        }

        .faq-question {
            flex-direction: column;
            text-align: left;
            gap: 1rem;
            padding-right: 2rem;
        }

        .faq-question h3 {
            font-size: 1.4rem;
            margin-bottom: 1rem;
            padding-right: 2.5rem;
        }

        .faq-question::after {
            right: 1rem;
            font-size: 1.8rem;
        }

        .faq-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .faq-answer p {
            font-size: 1rem;
            line-height: 1.6;
        }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const faqItems = document.querySelectorAll('.faq-item');
        
        faqItems.forEach(item => {
            item.addEventListener('click', function(e) {
                // Evitar que el clic se propague si es en la respuesta
                if (e.target.closest('.faq-answer')) {
                    return;
                }
                
                // Cerrar todas las otras respuestas
                faqItems.forEach(otherItem => {
                    if (otherItem !== item && otherItem.classList.contains('active')) {
                        otherItem.classList.remove('active');
                    }
                });
                
                // Alternar la clase active en el item actual
                item.classList.toggle('active');
                
                // Animar scroll suavemente si el item no está completamente visible
                if (item.classList.contains('active')) {
                    const itemRect = item.getBoundingClientRect();
                    const itemBottom = itemRect.bottom;
                    const windowHeight = window.innerHeight;
                    
                    if (itemBottom > windowHeight) {
                        const scrollTo = window.scrollY + itemRect.top - 100;
                        window.scrollTo({
                            top: scrollTo,
                            behavior: 'smooth'
                        });
                    }
                }
            });
        });
        
        // Cerrar respuesta al hacer clic fuera
        document.addEventListener('click', function(e) {
            if (!e.target.closest('.faq-item')) {
                faqItems.forEach(item => {
                    item.classList.remove('active');
                });
            }
        });
        
        // Prevenir que los clics dentro de la respuesta cierren el acordeón
        const faqAnswers = document.querySelectorAll('.faq-answer');
        faqAnswers.forEach(answer => {
            answer.addEventListener('click', function(e) {
                e.stopPropagation();
            });
        });
    });
</script>
