---
const { title } = Astro.props;
---
<section class="section">
    <h2 class="section-title">{title}</h2>
    <slot />
</section>

<style>
.section {
    margin-bottom: 4rem;
}

.section-title {
    color: var(--color-red);
    margin-bottom: 2rem;
    text-align: center;
}
</style>
